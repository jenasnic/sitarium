{#
Allows to view actor information.
Required :
- actor : actor we want to display information (as Actor).
#}

<div class="actor-details">

    <div>
        <p><strong>Nom complet</strong> : {{ actor.fullname }}</p>
        <p><strong>Date de naissance</strong> : {{ actor.birthdate|date('d-m-Y') }}</p>
        <p><img src="https://image.tmdb.org/t/p/w185/{{ actor.pictureUrl }}" alt="-" /></p>
        <p><strong>Identifiant TMDB</strong> : {{ actor.tmdbId }}</p>
    </div>

    {% if (actor.movies|length > 0) %}
    <div>
        <p><strong>Filmographie</strong> :</p>
        <ul>
        {% for movie in actor.movies %}
            <li>{{ movie.title }}</li>
        {% endfor %}
        </ul>
    </div>
    {% endif %}

</div>
